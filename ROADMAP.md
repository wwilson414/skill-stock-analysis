# Stock-Analysis Skill — Optimization Roadmap

> 基于已完成的回测实证（5 只跨市场股票、1005 个信号、2025–2026 窗口）与代码审计，
> 按优先级排列。核心实证结论：整体 IC -0.21 几乎全部来自 downtrend_decline（ic=-0.19）；
> 剔除下跌段后 IC 仅 ~+0.03；权重 A/B 证明调权无效——**问题在信号质量与市场状态，不在权重**。

## P0 — 扩大证据基础（先验证，再动手）

- [ ] **1. 扩样本验证**：20–50 只、跨行业跨市场（A股主板/创业板/科创、HK、US），
      回测窗口拉长到 2–3 年、多起点滚动。回答：负 IC 是窗口特异还是结构性缺陷。
- [ ] **2. 市场级 regime 标注**：当前 phase 只描述个股，还需按基准（沪深300/SPY）趋势
      给整个回测期打"牛市/熊市/震荡"标签，分段看 IC——个股 downtrend 在大盘牛市里含义完全不同。
- [ ] **3. IC 置信区间**：range_swing 段 n=158 太小。至少给 IC 加 bootstrap 区间或
      t-stat，避免对噪声过拟合（呼应 recalibrate_weights 的弱 IC 保护）。
- [ ] **4. 消除选择偏差**：当前样本股是"事后已知路径"的股票。改为随机/固定规则选股
      （如某日全市场按流动性抽样），或至少记录选股规则。

## P1 — 信号质量（真正的杠杆）

- [ ] **5. 下跌段均值回归信号（独立信号）**：实证显示 downtrend_decline 段 20d 平均
      收益 +1.01%（反弹存在）但动量评分无法排序（ic=-0.19）。做一个独立超跌反弹信号
      （如 RSI2 极值 + 放量止跌 + 距 MA60 乖离极值），**不要**混入动量总分。
- [ ] **6. 提升非下跌段 IC**（+0.03 → 目标 0.1+）：候选组件——换手率异动、量价背离
      确认、平台突破结构、跳空行为。每加一个组件用 by_phase 分段 IC 验证，无效即删。
- [ ] **7. 分数→概率校准**：用历史信号做 isotonic/logistic 回归，把 score_total 映射为
      "未来 N 日上涨概率"，取代拍脑袋的 75/60/45/30 信号阈值。输出语义从"75 分=买入"
      变为"历史上 68% 概率 20 日上涨"。
- [ ] **8. 风险调整 IC**：前向收益改为超额收益（已有 RS 基准）或除以同期 ATR，
      消除"高波动股票天然大 IC"的偏差。

## P2 — 执行真实性（回测可信度）

- [ ] **9. 交易成本与滑点**：A股卖出印花税、佣金、冲击成本计入前向收益；否则 IC 有
      系统性高估。
- [ ] **10. T+1 与涨跌停在回测中生效**：A股 forward return 应从 T+1 开盘起算；
      涨停封板的 buy 信号应剔除（买不进）——实盘路径已有 tradability，回测路径还没有。
- [ ] **11. 前向收益口径统一**：当前用收盘价算前向收益，与实盘"次日开盘买入"存在
      偏差；改为 next-open 入场价。

## P3 — 工程与可维护性

- [ ] **12. 数据缓存层**：回测每次重跑都重新拉取全量 K 线；加本地缓存（parquet/sqlite，
      按代码+日期增量更新），速度可提升一个数量级，也便于断点续跑。
- [ ] **13. 测试固化**：单测目前散在 /tmp 临时文件。建 `tests/` 目录，把各轮单测
      （对齐、复权、ATR、情绪、tradability、回测、A/B、phase）固化为 pytest 套件 + CI。
- [ ] **14. 阈值配置化**：评分系统里所有魔法数字（各类阈值、区间）集中到一个带理由
      注释的配置块/文件，改参数不用翻代码。
- [ ] **15. 回测结果落盘**：批量回测输出到数据库而非 stdout JSON，支持后续横向研究
      （跨股票/跨时期对比）。

## 明确不做 / 已否决

- ~~按回测 IC 直接改 `_DEFAULT_WEIGHTS`~~ — A/B 已证明无效，且反预测样本上危险。
- ~~给下跌段调低动量权重以"修复"负 IC~~ — 符号问题，权重只能改幅度。

## 决策树（后续执行的入口）

```
扩样本后 IC 结构如何？
├─ 下跌段仍 -0.19、非下跌段 +0.03 → 执行 P1-5（均值回归独立信号）+ P1-7（概率校准）
├─ 全段 IC 都 ≈ 0            → 评分框架推倒重来的信号，先做 P1-6 组件实验
└─ 全段 IC 转正（窗口特异）   → 仅需 P2 执行真实性 + 缓存，评分框架保留
```
